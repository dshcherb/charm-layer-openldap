name: openldap
summary: Stand-alone LDAP daemon (slapd) by openldap project
maintainer: dima <dima@blade>
description: |
  Slapd is the stand-alone LDAP daemon. It listens for LDAP connections
  on any number of ports (default 389), responding to the LDAP operations
  it receives over these connections.
tags:
  # Replace "misc" with one or more whitelisted tags from this list:
  # https://jujucharms.com/docs/stable/authors-charm-metadata
  - identity
  - idM
  - IdP
  - ldap
  - openldap
  - ops
series:
  - xenial
subordinate: false
# provides: null
# provides:
#  ldap-bind:
#    interface: ldap-bind
# requires: null
# peers: null
# peers:
#  slapd-peer:
#    interface: slapd-peer
